# -*- mode: snippet -*-
# author: Matthias
# name: getopts
# key: getopts
# --

while getopts "${1:a:}" opt; do
  case $opt in
    ${2:a})
        ${3:echo "-a was triggered with \${OPTARG}"}
      ;;
  esac
done


